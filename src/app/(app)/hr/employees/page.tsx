import { HrPlaceholder } from '../_components/hr-placeholder';

export default function HrEmployeesPage() {
    return <HrPlaceholder title="Employees" description="Browse employee records (admin-only in the new app)." />;
}

